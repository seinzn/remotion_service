version: "3.8"

services:
  redis:
    image: docker.1ms.run/redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  remotion:
    build: .
    ports:
      - "13926:13926"
    volumes:
      - ./public/output:/app/public/output
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
      - RENDER_CONCURRENCY=3
      - PORT=13926
    depends_on:
      - redis
    deploy:
      resources:
        limits:
          memory: 8G
          cpus: "4"

volumes:
  redis_data:
